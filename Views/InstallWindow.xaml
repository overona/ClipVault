<Window x:Class="ClipVault.Views.InstallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set up ClipVault" Width="460" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" Topmost="True" ShowInTaskbar="True"
        Background="{DynamicResource WindowBg}" Foreground="{DynamicResource Text}" FontFamily="Segoe UI" FontSize="13"
        TextOptions.TextFormattingMode="Display" UseLayoutRounding="True" SourceInitialized="Window_SourceInitialized">
    <Window.Resources>
        <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="Margin" Value="0,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <StackPanel Margin="20">
        <DockPanel>
            <Image Source="pack://application:,,,/Assets/ClipVault.ico" Width="40" Height="40" Margin="0,0,14,0" VerticalAlignment="Top"/>
            <StackPanel>
                <TextBlock x:Name="HeaderText" FontWeight="SemiBold" FontSize="15" TextWrapping="Wrap"/>
                <TextBlock x:Name="BodyText" TextWrapping="Wrap" Margin="0,6,0,0" Foreground="{DynamicResource Muted}"/>
            </StackPanel>
        </DockPanel>

        <StackPanel Margin="0,16,0,0">
            <CheckBox x:Name="StartWithWindowsBox" Content="Start ClipVault when I sign in to Windows" IsChecked="True"/>
            <CheckBox x:Name="ShortcutBox" Content="Add a Start menu shortcut" IsChecked="True"/>
        </StackPanel>

        <TextBlock x:Name="LocationText" TextWrapping="Wrap" Margin="0,10,0,0" FontSize="11.5" Foreground="{DynamicResource Muted}"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="SkipButton" Content="Run from here" Style="{StaticResource TextButton}" IsCancel="True" MinWidth="110" Margin="0,0,8,0"/>
            <Button x:Name="InstallButton" Content="Install" Style="{StaticResource PrimaryButton}" IsDefault="True" MinWidth="110" Click="Install_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
